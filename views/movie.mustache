<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <title>{{movie.title}}</title>
  <link rel="stylesheet" href="/css/movies.css">
</head>
<body>

<!-- Header / Meny -->
<header class="header">
  <div class="header_inner">
    <a href="/" class="logo"><img src="/img/Exclude.png" alt="Kino"></a>
    <nav class="nav_desktop">
      <ul class="nav_list">
        <li class="nav_item"><a href="/" class="nav_link">Startsida</a></li>
        <li class="nav_item"><a href="/movies" class="nav_link">Filmer</a></li>
        <li class="nav_item"><a href="/about" class="nav_link">Om biografen</a></li>
      </ul>
    </nav>
  </div>
</header>

<a href="/movies" class="back">← Tillbaka till alla filmer</a>

<article class="movie">
  <h1>{{movie.title}}</h1>

  <!-- Rendera intro med Markdown -->
  {{#movie.introHTML}}
    <div class="intro">{{{movie.introHTML}}}</div>
  {{/movie.introHTML}}

  <!-- Bild -->
  {{#movie.image}}
    <img src="{{movie.image.url}}" alt="{{movie.title}}">
  {{/movie.image}}

  <!-- Valfria fält -->
  {{#movie.description}}
    <p><strong>Beskrivning:</strong> {{{movie.description}}}</p>
  {{/movie.description}}

  {{#movie.year}}
    <p><strong>År:</strong> {{movie.year}}</p>
  {{/movie.year}}
</article>

</body>
</html>
